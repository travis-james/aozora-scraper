Unzipping JP zipfiles.
unzip -O shift-jis fire.zip

*** Assumptions ***
All links to works on an author page start with "../cards" and therefore all links of a given
work are:
https://www.aozora.gr.jp + "/cards"


	b := "./files/2574_ruby_32344.zip"
	a := "https://www.aozora.gr.jp/cards/000020/card2574.html"
	b = strings.TrimLeft(b, "./")
	c := strings.Split(a, "/")
	retv := c[0] + "//" + c[2] + "/" + c[3] + "/" + c[4] + "/" + b
	for i, v := range c {
		fmt.Println(i, v)
	}
	fmt.Println(retv)